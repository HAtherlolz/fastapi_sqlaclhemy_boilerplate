from fastapi import APIRouter

# import endpoints routers from v1

api_router = APIRouter()

# include routers from v1

# api_router.include_router(router_from_v1, prefix='/api/v1', tags=["endpoints_for"])
